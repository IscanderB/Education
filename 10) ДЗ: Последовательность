using System;

namespace Study
{
    internal class Program
    {
        static void Main(string[] args)
        {
            int startNumber = 5,
                finalNimber = 103,
                step = 7,
                result = startNumber;

            while (result < (finalNimber + step))
            {
                Console.WriteLine(result);
                result += step;
            }
        }
    }
}
